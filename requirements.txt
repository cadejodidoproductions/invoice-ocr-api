# Invoice OCR API Dependencies

# Core Framework
fastapi>=0.100.0
uvicorn[standard]>=0.23.0

# PDF Processing
pypdf>=3.0.0

# File Upload Handling
python-multipart>=0.0.6

# HTTP Client (for webhooks and external API calls)
httpx>=0.24.0

# =============================================================================
# Optional: OCR Support (requires system dependencies)
# Install Tesseract: apt-get install tesseract-ocr
# Install Poppler: apt-get install poppler-utils
# =============================================================================
pytesseract>=0.3.10
Pillow>=10.0.0
pdf2image>=1.16.0

# =============================================================================
# Optional: Distributed Job Queue (requires Redis)
# =============================================================================
celery[redis]>=5.3.0

# =============================================================================
# Optional: ML-based Extraction (for improved accuracy)
# Requires additional disk space for model downloads (~500MB)
# =============================================================================
spacy>=3.7.0
transformers>=4.35.0
torch>=2.0.0
